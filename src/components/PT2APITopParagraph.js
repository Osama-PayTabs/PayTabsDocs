import React from 'react';

export default function PT2APITopParagraph({section}) {
    return (
    <p>
        PayTabs provides you with a collection of API endpoints which used to process all payments, regardless of if they are through either your own payment pages, the managed payment pages, or if you are using the {section}.
    </p>
  );
}