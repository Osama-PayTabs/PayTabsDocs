import React from 'react';

export default function Layout_7_4_APIsRefundTransaction({section}) {
    return (
        <p>
            Building your business from scratch and can't find any of the above solutions compatible with your business? Don't worry we got your back. PayTabs provides you with a collection of API endpoints which used to process all payments, regardless of if they are through either your own payment pages, the managed payment pages, or if you are using the {section}.
        </p>
  );
}